{
  "apiVersion": "apps/v1",
  "kind": "Deployment",
  "metadata": {
    "namespace": "websites"
  },
  "spec": {
    "selector": {
      "matchLabels": {
        "app": "cueTest"
      }
    },
    "minReadySeconds": 5,
    "template": {
      "metadata": {
        "labels": {
          "app": "cueTest"
        }
      },
      "spec": {
        "containers": [
          {
            "name": "website",
            "image": "nginx:alpine",
            "imagePullPolicy": "Always",
            "ports": [
              {
                "containerPort": 80,
                "protocol": "TCP"
              }
            ]

          }
        ]
      }
    }
  }
}